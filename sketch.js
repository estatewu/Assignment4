function setup() {  // Create canvas, set angle mode, etc.  createCanvas(windowWidth,windowHeight);  angleMode(DEGREES);}function draw() {  // Do things while call clockhand()  background(color('pink'));  if(width>height){    c=height;  }  else {  c=width;  }  translate(width/2,height/2);  clockHand(hour()-12,184*c/600,12,30*c/600);   clockHand(minute(),127*c/600,60,10*c/600);  clockHand(second(),75*c/600,60,5*c/600);      fill(255);  textSize(12);  text(clockText(),0,0);}function clockHand(timevalue,radius,range,l) {  push();  fill(color('white'));  noStroke();  for(var angle = 360/range; angle <= 360; angle+= 360/range) {   ellipse(radius*cos(angle),radius*sin(angle),l,l);    }  fill(color('gray'));  var a = map(timevalue,0,range,-90,270);  ellipse(radius*cos(a),radius*sin(a),l,l);  fill(color('pink'));  for(var b = -90; b < a - 1; b+= 360/range){    ellipse(radius*cos(b),radius*sin(b),l,l);  }  pop();  }function clockText() {  var h = hour();  var m2 = minute();  var s = second();  if(m2<10){    m2 = "0"+minute();  }else{    m2=m2  }  if(s<10){    s = "0"+second();  }else{    s=s  }  if (h> 12) {      if(h>22){       return (h-12)+" : "+m2+" : "+s+"   PM";      }else{       return "0"+ (h-12)+" : "+m2+" : "+s+"   PM";      }  }else{    return h+" : "+m2+" : "+s+"   AM";  }}function windowResized() {  resizeCanvas(windowWidth, windowHeight);}